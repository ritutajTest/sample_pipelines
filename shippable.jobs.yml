jobs:
  - name: x86-64-ubuntu-container-shipctl
    type: runSh
    runtime:
      architecture: x86_64
      os: Ubuntu_16.04
      container: true
    flags:
      - x86-64
      - ubuntu16
    steps:
      - TASK:
          name: shipctl-should-work
          script:
            - echo "hello"
            - echo "hello all"
            - shipctl retry echo "it works"
