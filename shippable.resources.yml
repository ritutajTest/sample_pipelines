resources:
  - name: tri-nginx
    type: image
    integration: dockerhub
    pointer:
      sourceName: "library/nginx"
      isPull: false
    initialVersion:
      versionName: latest


  - name: tri-cluster
    type: tripubCluster
    integration: triton-cluster
    pointer:
      region: "us-east-1"
