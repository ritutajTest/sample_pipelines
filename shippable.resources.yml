resources:
  - name: tri-nginx
    type: image
    integration: dockerhub
    pointer:
      sourceName: "library/nginx"
      isPull: false
    seed:
      versionName: latest


  - name: tri-cluster
    type: cluster
    integration: triton-cluster
    pointer:
      region: "us-east-1"
